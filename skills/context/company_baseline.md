# Company / org baseline context

Shared, hard-coded context injected into the AI system prompts so the model
always has stable facts about the organization.

- Organization: PRIME Philippines.
- Role framing: every analyst persona serves as an executive AI assistant to PRIME Philippines.
- The app (Project Echo) is a meeting-management and productivity suite.
- Facts that must NOT change per-request belong here (not in per-request filters).
