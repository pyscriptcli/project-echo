# components/calendar_filters.py
import streamlit as st

def render_task_calendar_filters():
    """
    Returns a dict of filter values.
    Renders a single tight row with:
    - Assignee dropdown (people + groups + unassigned)
    - Status checkboxes (todo, in_progress, done)
    - Meeting linked search (text input)
    - Unscheduled toggle (show/hide tasks without due date)
    All with inline SVG icons, minimal padding.
    """
    pass
